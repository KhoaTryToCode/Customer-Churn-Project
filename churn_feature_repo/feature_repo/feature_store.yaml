
project: churn_feature_repo
registry: data/registry.db
provider: local
online_store:
    type: redis
    connection_string: "localhost:6379" # Assumes Redis is running locally via Docker
entity_key_serialization_version: 3
